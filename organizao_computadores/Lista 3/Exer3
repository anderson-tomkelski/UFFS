Cada * corresponde a uma entrada que espera por uma saída

add $3, $4, $6
sub $5, $3, $2
lw $7, 100($5)
add $8, $7, $2

* = $s3
** = $s5
*** = $s7

fetch -> reg -> ALU ->* MEM -> REG
         fetch -> reg ->* ALU ->** MEM -> REG
	          fetch -> reg ->** ALU ->*** MEM -> REG
			   fetch -> reg -> ***ALU -> MEM ->REG
